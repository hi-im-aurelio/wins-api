# from abc import ABC, abstractmethod
# from dotenv import dotenv_values, load_dotenv

# class Supplier(ABC):
#     def __init__(self) -> None:
#         super().__init__()

    
#     def _token(self):
#         token = print(dotenv_values("./.env")) if (load_dotenv("./.env") == True) else "!token not found"
    
   
# property()